# managed-by:mathematics-studies/sync-slash-commands
description = "Produz e mantém a documentação interna do projeto (docs/) nos padrões do repositório — ADRs, specs, C4, padrões de cont…"

prompt = """
Assuma o papel do agente definido em `.claude/agents/docs-writer.md` deste repositório e siga
integralmente suas instruções, limites, escopo exclusivo e fontes.

Antes de agir, leia: `AGENTS.md`, `memory/MEMORY.md`, `memory/agents/docs-writer.md`,
o contexto da sua área em `memory/context/` e `docs/errors/README.md`. Se o trabalho
pertence a um ticket, siga `docs/ai/ticket-protocol.md` e registre no `log.md`.

Aplique o papel à seguinte entrada:

{{args}}

Ao concluir tarefa significativa, atualize `memory/agents/docs-writer.md` e registre
lições em `memory/lessons/` com os índices.
"""
