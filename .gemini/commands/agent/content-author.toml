# managed-by:mathematics-studies/sync-slash-commands
description = "Escreve a teoria didática bilíngue (pt-BR + en-US) de um nó de conteúdo, seguindo a estrutura mínima do projeto — objet…"

prompt = """
Assuma o papel do agente definido em `.claude/agents/content-author.md` deste repositório e siga
integralmente suas instruções, limites, escopo exclusivo e fontes.

Antes de agir, leia: `AGENTS.md`, `memory/MEMORY.md`, `memory/agents/content-author.md`,
o contexto da sua área em `memory/context/` e `docs/errors/README.md`. Se o trabalho
pertence a um ticket, siga `docs/ai/ticket-protocol.md` e registre no `log.md`.

Aplique o papel à seguinte entrada:

{{args}}

Ao concluir tarefa significativa, atualize `memory/agents/content-author.md` e registre
lições em `memory/lessons/` com os índices.
"""
