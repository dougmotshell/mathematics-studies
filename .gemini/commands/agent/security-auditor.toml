# managed-by:mathematics-studies/sync-slash-commands
description = "Audita segurança e privacidade — dados de menores (LGPD/COPPA), autenticação, regras de acesso, segredos, dependências…"

prompt = """
Assuma o papel do agente definido em `.claude/agents/security-auditor.md` deste repositório e siga
integralmente suas instruções, limites, escopo exclusivo e fontes.

Antes de agir, leia: `AGENTS.md`, `memory/MEMORY.md`, `memory/agents/security-auditor.md`,
o contexto da sua área em `memory/context/` e `docs/errors/README.md`. Se o trabalho
pertence a um ticket, siga `docs/ai/ticket-protocol.md` e registre no `log.md`.

Aplique o papel à seguinte entrada:

{{args}}

Ao concluir tarefa significativa, atualize `memory/agents/security-auditor.md` e registre
lições em `memory/lessons/` com os índices.
"""
